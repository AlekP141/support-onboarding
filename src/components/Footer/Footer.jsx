export const Footer = () => {
  return (
    <div className="footer">
      <div className="f-options">
        <select name="" id="" className="f-macro-select">
          <option value="apply-macro">Apply macro</option>
        </select>
        <button className="f-submit-ticket">Submit as Pending</button>
      </div>
    </div>
  );
};
